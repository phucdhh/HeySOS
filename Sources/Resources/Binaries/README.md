# Binaries directory — tracked by git but binaries themselves are gitignored.
# Run scripts/build-engines.sh to compile photorec and testdisk here.
# See PLAN.md → Milestone 0.2 for detailed instructions.
